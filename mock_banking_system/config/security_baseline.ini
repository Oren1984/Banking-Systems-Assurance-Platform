; SYNTHETIC MOCK BANKING SYSTEM — fake application-security baseline config, not real.
; Deliberately planted findings — see docs/mock_banking_planted_findings.md.

[app]
DEBUG = true
; expect CFG-001 (MEDIUM)

[network]
verify_ssl = false
; expect CFG-002 (HIGH)
host = 0.0.0.0
; expect CFG-006 (MEDIUM)

[auth]
auth_enabled = false
; expect CFG-003 (CRITICAL)
admin = admin
; expect CFG-007 (HIGH)

[cors]
Access-Control-Allow-Origin = *
; expect CFG-005 (MEDIUM)

[errors]
display_errors = true
; expect CFG-008 (LOW)
