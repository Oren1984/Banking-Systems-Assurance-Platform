# SYNTHETIC MOCK BANKING SYSTEM — fake data retention/privacy configuration, not real code.
# Deliberately planted finding — see docs/mock_banking_planted_findings.md.

encrypt_enabled = false  # expect CFG-004 (encryption appears disabled) — pseudo-config value, not valid Python; never executed, only scanned as text
