# controls/

Not implemented in Phase 1. Planned for Phase 3/4 — control definitions and control-to-domain
mapping (`models.enums.ControlType`, `core.domains.BankingDomain`). See
`BANKING_PLATFORM_INTEGRATION_PLAN.md` §11 (`Control` schema) and §9 (required separation
between technical controls / internal policies / architecture standards / business rules /
regulatory references).
