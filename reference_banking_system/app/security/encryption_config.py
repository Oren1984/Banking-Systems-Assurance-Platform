# SYNTHETIC REFERENCE BANKING SYSTEM — fake encryption configuration, not real code.
# Deliberate positive example: encryption and TLS verification both enabled.

encryption_enabled = True
tls_verify = True
